import React from 'react';
import styled from 'styled-components';
import PropTypes from 'prop-types';
import { Form } from '../Common';

const GroupForm = () => {
  return <Form>Lucas</Form>;
};

export default GroupForm;
